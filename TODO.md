- [ ] Add a “Preloaded Choice Samples” section in `home/ubuntu/index.html` for displaying curated Splice samples
- [ ] Implement “best” sample curation (heuristic-based) in `home/ubuntu/app.js` after Splice samples load
- [ ] Render curated preloaded samples into the new section while keeping full sample browsing list separate
- [ ] Make AI payloads able to pull from the preloaded sample list (include choice samples in AI requests)
- [ ] Quick manual verification: load app, confirm preloaded section populates, drag/drop works, AI finds samples from the preloaded pool
