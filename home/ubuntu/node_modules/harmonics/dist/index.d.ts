/**
 *
 * @param {String} rootChord_Oct e.g. CM or CM_5 Fmaj7 or Dbb9sus or Db9sus
 * @returns Array
 * Take an inline chord such as CM or Csus4_3 and return an array of it's notes
 * Used in Scribbletune to allow adding chords inline with notes
 */
declare function inlineChord(rootChord_Oct: string): string[];
declare function chords(): string[];
declare function scales(): string[];
declare function chord(chord: string): string[];
declare function scale(scale: string): string[];
/**
 *
 * @param {String} scaleOrBitmap e.g '110010110011' or 'phrygian'
 * @returns {Array} e.g. [0, 1,  3,  5, 7, 8, 10, 12]
 */
declare function getIndicesFromScale(scaleOrBitmap: string): number[];

export { chord, chords, getIndicesFromScale, inlineChord, scale, scales };
